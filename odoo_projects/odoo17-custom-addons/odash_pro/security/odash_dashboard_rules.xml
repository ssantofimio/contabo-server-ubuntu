<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Record rule to restrict employees to their own dashboards -->
    <record id="odash_dashboard_user_rule" model="ir.rule">
        <field name="name">Dashboard: Users can only access their own dashboards</field>
        <field name="model_id" ref="model_odash_pro_dashboard"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- System administrators have full access to all dashboards -->
    <record id="odash_dashboard_system_rule" model="ir.rule">
        <field name="name">Dashboard: System administrators have full access</field>
        <field name="model_id" ref="model_odash_pro_dashboard"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('base.group_system'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- Record rule to only see odash_pro.config for odash_pro.editor -->
    <record id="odash_config_editor_rule" model="ir.rule">
        <field name="name">Config: Only editor can see config</field>
        <field name="model_id" ref="model_odash_pro_config"/>
        <field name="domain_force">[('create_uid', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('odash_pro.group_odash_pro_editor'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>
